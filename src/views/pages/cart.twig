{% extends "layouts.master" %}

{% block content %}
<div class="cart-page">
    <div class="container mx-auto px-4 py-8">
        <h1 class="text-3xl font-bold mb-8">{{ trans('cart.your_cart') }}</h1>
        
        <div class="cart-content">
            <salla-cart-page></salla-cart-page>
        </div>
    </div>
</div>
{% endblock %}
